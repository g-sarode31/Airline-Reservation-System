# Airline Reservation Frontend (HTML/CSS/JS)

## Now Enhanced
- Pro visual redesign with custom SVG assets and interactive UI
- New pages: `destinations.html`, `offers.html`, `about.html`, `contact.html`
- Improved navbar links across public pages
- Added destination region filters
- Added flash-deal countdown timer
- Added one-click promo code copy
- Added admin analytics charts (Chart.js): monthly bookings + revenue trends
- Added admin flight route map visualization (Leaflet.js) with route selector
- Added animated counters and card tilt micro-interactions
- Added floating help CTA and richer landing sections

## Existing Full Flow
- User: register, login, flight search, results, booking, seat selection, payment simulation, dashboard, cancel, ticket download
- Admin: login, dashboard metrics, flight CRUD, bookings view, users management

## Run
1. Open `index.html` in browser (or run with Live Server).
2. Use admin login at `admin/login.html`.

## Default Admin
- Email: `admin@skywings.com`
- Password: `admin123`

## Storage
- Data is persisted in browser `localStorage`.

